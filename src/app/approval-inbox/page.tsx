'use client';
import React from 'react';
import AppLayout from '@/components/AppLayout';
import ApprovalInboxContent from './components/ApprovalInboxContent';

export default function ApprovalInboxPage() {
  return (
    <AppLayout>
      <ApprovalInboxContent />
    </AppLayout>
  );
}
